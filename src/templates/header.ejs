<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle %> </title>

  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
      font-size: 1rem;
      font-weight: 400;
      line-height: 1.5;
      text-align: left;
    }

    .form .form__field {
      margin-bottom: .5rem;
    }

    .form .form__label {
      font-weight: bold;
      margin-right: .25rem;
    }

    .header__link:not(:last-child) {
      margin-right: .5rem;
    }

  </style>
</head>

<body>
  <header class="header">
    <% if (typeof req !== 'undefined' && req.user) { %>
    <a href="/products/add" class="header__link">
      Add product
    </a>

    <span>
      You are signed in as <strong><%= req.user.fullName %></strong>
      <form action="/logout" method="post" novalidate style="display: inline-block;">
        <button type="submit">
          Logout
        </button>
      </form>
    </span>
    <% } else { %>
    <a href="/sign-in" class="header__link">
      Sign In
    </a>

    <a href="/sign-up" class="header__link">
      Sign Up
    </a>
    <% } %>
  </header>
